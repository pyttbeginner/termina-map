# Fear & Hunger 2: Termina — Интерактивная карта

Интерактивная карта Прехевила и окрестностей для игры Fear & Hunger 2: Termina.

## 🎮 Как использовать

1. **Открой сайт** (локально или на GitHub Pages)
2. **Кликни на зону** на карте — откроется детальная карта локации
3. **Кликни на метку здания** (синяя) — откроется план здания со всеми этажами

## 🎯 Управление

| Действие | Клавиша / Элемент |
|----------|-------------------|
| Режим редактора | **E** (или **У**) |
| Перемещение зоны | Перетащить мышкой (в режиме редактора) |
| Изменение размера зоны | Тянуть за правый нижний угол |
| Закрыть модальное окно | **Esc** или клик вне окна |

## 📁 Структура проекта

```
fear-hunger-maps/
├── index.html              # Главная страница
├── css/
│   └── style.css           # Стили
├── js/
│   └── map.js              # Логика карты
├── data/
│   └── markers.json        # Метки (враги, сундуки, кровати, здания)
├── maps_outdor/            # Карты улиц/локаций (PNG)
├── maps_indoor/            # Карты помещений/зданий
└── download_maps.py        # Скрипт для парсинга (опционально)
```

## 🗺️ Добавление меток

1. Открой любую локацию (клик по зоне на главной карте)
2. Нажми **⚔️ Враг**, **📦 Сундук**, **🛏️ Кровать**, или **🏠 Здание**
3. Кликни по карте для установки метки
4. Введи название и (для зданий) ID здания
5. Нажми **💾 Экспорт меток** — скачается `markers.json`
6. Замени файл `data/markers.json` на новый

## 🏰 ID зданий

Полный список ID зданий находится в `js/map.js` (объект `buildings`).

Основные: `tavern`, `church`, `department_store`, `museum`, `mayors_manor`, `orphanage`, `sewers`, `train`, и др.

## 🛠️ Редактирование зон

1. Нажми **E** для входа в режим редактора
2. Перетаскивай зоны мышкой
3. Тяни за правый нижний угол для изменения размера
4. Двойной клик по зоне для редактирования названия
5. Нажми **💾 Экспорт зон (JSON)** для сохранения
6. Обнови массив `worldZones` в `js/map.js`

## 🚀 Размещение на GitHub Pages

1. Создай репозиторий на GitHub
2. Запушь файлы:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/USERNAME/fear-hunger-maps.git
   git push -u origin main
   ```
3. В репозитории: **Settings → Pages → Source: main branch**
4. Сайт будет доступен по ссылке вида: `https://USERNAME.github.io/fear-hunger-maps/`

## 📝 Заметки

- Все изображения локаций должны быть в формате **PNG** с прозрачным фоном
- Картинки оптимизированы для веба (ленивая загрузка)
- Для добавления новых зон используй визуальный редактор (клавиша E)

## 📄 Лицензия

Фан-проект. Все права на изображения и игру принадлежат разработчикам Fear & Hunger 2: Termina.
